////////////////////////////
/*  State initialization  */
////////////////////////////
$.each($.state_config, function(index, state) {
   // Instantiate a new state and define all it's default functions
});